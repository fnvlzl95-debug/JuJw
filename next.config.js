/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export',
  images: {
    unoptimized: true,
  },
  basePath: '/JuJw',
  assetPrefix: '/JuJw/',
}

module.exports = nextConfig
